#+OPTIONS: ^:nil
* HARMer DRL: Simulation-Driven RL for Automated Penetration Testing

This repository contains a full research pipeline for reinforcement learning (RL)
in automated penetration testing. It includes:
- multi-subnet simulation environments,
- baseline and extended RL agents,
- reward-failure diagnosis and fixes,
- generalization and few-shot adaptation experiments,
- real-experiment preparation and live-lab execution scripts,
- paper artifacts (figures, tables, and IEEE-style manuscript source).

* Paper at a Glance

** Key contributions
- Dead-loop diagnosis and reward-level fix for value-based agents (DQN/DDQN).
- Generalization benchmark on unseen networks (X/Y/Z) with few-shot adaptation.
- Unified comparison across RL families (Q-Learning, DQN/DDQN, PPO, A2C/A3C-like, DPPO-like, GAIL-like) plus reproducible real-experiment logging assets.

** Where to read the paper source
- Manuscript: `paper_overleaf/main.tex`
- References: `paper_overleaf/references.bib`

** Main result artifacts
- Main simulation comparison: `results_compare_complex_simulation.csv`
- Transfer benchmark (X/Y/Z): `results_transfer_xyz.csv`
- Generalization + adaptation: `results_generalization_adaptation.csv`
- Real experiment run history: `real_experiments/results/experiment_summary.md`

* Repository Highlights

** Core simulation and RL training
- `pentest_env.py`, `pentest_env_complex.py`, `pentest_env_fixed.py`
- `train_q_learning.py`, `train_dqn.py`, `train_a2c.py`, `train_complex.py`
- `compare_complex.py`, `evaluate_transfer_networks.py`, `generalize_and_adapt.py`

** Extended methods
- A3C-like: `a3c_pytorch.py`, `train_a3c_like_only.py`
- DPPO-like and GAIL-like: `simulation_extensions.py`, `train_simulation_extensions.py`

** Real-experiment assets
- Network configs and runbooks: `real_experiments/configs/`, `real_experiments/runbooks/`
- Live/dry-run orchestrator: `real_experiments/run_real_experiment.py`
- Result logs: `real_experiments/results/`

** Paper artifacts
- Manuscript source: `paper_overleaf/main.tex`
- Bibliography: `paper_overleaf/references.bib`
- Figures used in paper: `paper_overleaf/figures/`

* Environment Setup

** Option A: Virtual environment (recommended)
#+begin_example
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -r requirements.txt
#+end_example

If `requirements.txt` is not available in your clone, use Pipenv:
#+begin_example
pip install pipenv
pipenv install --dev
pipenv shell
#+end_example

* Quick Start

** 1) Train/compare in simulation
#+begin_example
python compare_complex.py
#+end_example

** 2) Run generalization + adaptation benchmark
#+begin_example
python generalize_and_adapt.py
#+end_example

** 3) Train extension agents (DPPO-like / GAIL-like)
#+begin_example
python train_simulation_extensions.py
#+end_example

* Real-Experiment Workflow

This project separates simulation claims from live execution claims.

** Dry-run validation
#+begin_example
cd real_experiments
python run_real_experiment.py --network alpha --mode dry-run
#+end_example

** Live lab run (authorized environment only)
#+begin_example
cd real_experiments
python run_real_experiment.py --network lab --mode live
#+end_example

Docker lab assets are under `docker_env/`.

* Key Outputs

** Simulation and adaptation
- `results_compare_complex_simulation.csv`
- `results_transfer_xyz.csv`
- `results_generalization_adaptation.csv`

** Real experiments
- `real_experiments/results/real_experiments_results.csv`
- `real_experiments/results/real_experiments_execution_log.json`
- `real_experiments/results/runs/` (per-run JSON reports)
- `real_experiments/results/experiment_summary.md`

* Reproducibility Notes

- Random seeds and run settings are defined in training/evaluation scripts.
- TensorBoard outputs are generated in `*_tensorboard/` folders.
- Model checkpoints are stored under `models*` directories.

* Ethics and Safety

Use this repository only in authorized environments. Do not run live exploitation
against systems without explicit permission.
